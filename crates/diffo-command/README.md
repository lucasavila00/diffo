# diffo-command

`diffo-command` provides the reusable command palette used by Diffo tools.

It owns command identity, palette state, keyboard and mouse input handling, layout,
and rendering. Command execution remains the responsibility of the calling
application.
