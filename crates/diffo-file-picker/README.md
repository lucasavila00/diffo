# diffo-file-picker

`diffo-file-picker` provides the shared file-picker panel used by Diffo activities.

It supports flat and tree documents, selection and scrolling state, fixed keyboard
and mouse navigation, and terminal rendering. Callers provide stable row keys and
handle activation outcomes.
