# diffo-diff

`diffo-diff` parses unified patches and builds the row projections rendered by
Diffo.

It provides the shared diff document types and both inline and side-by-side
projections. The crate does not perform Git I/O or terminal rendering.
